import React from 'react';
import { View, Text } from 'react-native';

const Transcription = () => {
  return (
    <View>
      <Text>Transcription Component</Text>
    </View>
  );
};

export default Transcription;